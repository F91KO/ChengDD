# 当前任务收口清单

## 当前状态
- 统计时间：2026-03-22
- 当前分支：`feature-auth-persistence`
- 当前情况：本地工作区已集成多项一期能力，但尚未按 issue 维度完成提交、PR 与关闭。
- 自动化验收：见 [phase1-acceptance-latest.md](/Volumes/workspace/ChengDD/docs/05-delivery/reports/phase1-acceptance-latest.md)，当前报告为通过。

## GitHub issue 对应状态
| Issue | 主题 | 当前判断 | 主要落地模块 |
| --- | --- | --- | --- |
| #18 | 自建 JWT 认证基线 | 已完成，待收口 | `cdd-auth-service`、`cdd-common-security`、`cdd-gateway` |
| #21 | 商户入驻与激活基线 | 已完成，待收口 | `cdd-api-merchant`、`cdd-merchant-service`、`db/migration/V12__merchant_onboarding_baseline.sql` |
| #22 | 支付回调、退款、补偿联动基线 | 已完成，待收口 | `cdd-api-order`、`cdd-order-service`、`db/migration/V16__order_baseline.sql` |
| #23 | 订单结算与生命周期基线 | 已完成，待收口 | `cdd-api-order`、`cdd-order-service`、`db/migration/V16__order_baseline.sql` |
| #24 | 商品目录与库存基线 | 已完成，待收口 | `cdd-api-product`、`cdd-product-service`、`db/migration/V15__product_catalog_baseline.sql` |
| #25 | 类目模板与商家类目树基线 | 已完成，待收口 | `cdd-api-product`、`cdd-product-service`、`db/migration/V15__product_catalog_baseline.sql` |
| #26 | 发布治理与版本映射基线 | 已完成，待收口 | `cdd-api-release`、`cdd-release-service`、`db/migration/V13__release_governance_baseline.sql` |
| #27 | 一期自动化测试与验收基线 | 已完成，待收口 | `scripts/testing`、`scripts/validation`、`docs/05-delivery/reports` |
| #28 | Agent Core 基线 | 已完成，待收口 | `cdd-agent-core`、`cdd-parent/pom.xml` |
| #29 | 数据权限与 RBAC 基线 | 已完成，待收口 | `cdd-common-security` |
| #30 | 配置与功能开关管理基线 | 已完成，待收口 | `cdd-api-config`、`cdd-config-service`、`db/migration/V14__config_switches_baseline.sql` |
| #31 | 商品明细退货退款基线 | 未开始 | 下一迭代 |

## 已确认的实现线索
- 认证与权限：
  - `cdd-auth-service` 已提供登录、刷新、登出、`/me` 接口。
  - `cdd-common-security` 已包含 JWT、RBAC、数据范围拦截与注解。
- 商户：
  - `cdd-merchant-service` 已提供申请、提交、审核、小程序校验、激活接口。
- 商品：
  - `cdd-product-service` 已包含类目模板、类目树、商品、上下架、库存调整接口。
- 订单：
  - `cdd-order-service` 已包含购物车、结算、下单、支付中、支付成功、支付回调、退款申请、退款回调、取消、履约、详情、列表、状态日志接口。
  - 已补充订单级部分退款累计控制。
- 发布：
  - `cdd-release-service` 已包含发布任务创建、状态流转、步骤更新、结果同步、回滚接口。
- 配置：
  - `cdd-config-service` 已包含平台配置、商家覆盖、功能开关定义、启停、查询接口。
- Agent：
  - `cdd-agent-core` 已包含任务定义、执行、审批、审计、补偿等基础模型与测试。
- 数据库：
  - `config/db-migration/db.changelog-master.yaml` 已接入 `V12` 到 `V16`。

## 建议提交批次

### 批次 A：认证与权限
- 对应 issue：`#18`、`#29`
- 建议范围：
  - `cdd-auth-service`
  - `cdd-common-security`
  - `cdd-gateway`
- 验证动作：
  - 认证、网关、权限相关模块测试
  - 网关启动校验

### 批次 B：商户入驻
- 对应 issue：`#21`
- 建议范围：
  - `cdd-api-merchant`
  - `cdd-merchant-service`
  - `db/migration/V12__merchant_onboarding_baseline.sql`
  - `config/db-migration/db.changelog-master.yaml`
- 验证动作：
  - 商户服务测试
  - 本地启动验证

### 批次 C：商品目录与类目
- 对应 issue：`#24`、`#25`
- 建议范围：
  - `cdd-api-product`
  - `cdd-product-service`
  - `db/migration/V15__product_catalog_baseline.sql`
  - `config/db-migration/db.changelog-master.yaml`
- 说明：
  - 商品目录与类目模板共享同一服务与迁移脚本，建议同批提交。
- 验证动作：
  - 商品服务测试
  - 商品服务启动验证

### 批次 D：订单、支付回调、退款补偿
- 对应 issue：`#22`、`#23`
- 建议范围：
  - `cdd-api-order`
  - `cdd-order-service`
  - `db/migration/V16__order_baseline.sql`
  - `config/db-migration/db.changelog-master.yaml`
- 说明：
  - 订单主链路与退款补偿高度耦合，建议同批提交。
  - 当前已补上订单级部分退款累计控制，但商品明细退货退款留在 `#31`。
- 验证动作：
  - `cdd-order-service` 测试
  - 本地启动验证

### 批次 E：发布治理
- 对应 issue：`#26`
- 建议范围：
  - `cdd-api-release`
  - `cdd-release-service`
  - `db/migration/V13__release_governance_baseline.sql`
  - `config/db-migration/db.changelog-master.yaml`
- 验证动作：
  - 发布服务测试
  - 本地启动验证

### 批次 F：配置与功能开关
- 对应 issue：`#30`
- 建议范围：
  - `cdd-api-config`
  - `cdd-config-service`
  - `db/migration/V14__config_switches_baseline.sql`
  - `config/db-migration/db.changelog-master.yaml`
- 验证动作：
  - 配置服务测试
  - 本地启动验证

### 批次 G：Agent Core
- 对应 issue：`#28`
- 建议范围：
  - `cdd-agent-core`
  - `cdd-parent/pom.xml`
- 验证动作：
  - `cdd-agent-core` 测试
  - 父工程编译校验

### 批次 H：验收、脚本与交付文档
- 对应 issue：`#27`
- 建议范围：
  - `scripts/testing`
  - `scripts/validation`
  - `docs/05-delivery/reports`
  - `docs/05-delivery`
  - `docs/02-architecture`
  - `docs/README.md`
  - `.gitignore`
- 验证动作：
  - 一期验收脚本
  - 文档目录检查

## 批次依赖关系
- `批次 G` 应先于其他服务批次收口，因为父工程新增了 `cdd-agent-core` 模块。
- `批次 A` 建议优先，因为其他服务的鉴权与数据权限说明依赖该能力。
- `批次 B`、`批次 C`、`批次 D`、`批次 E`、`批次 F` 可按服务边界依次收口。
- `批次 H` 放在最后，统一补齐报告、说明和索引。

## 当前不建议直接关闭的 issue
- `#31`
  - 原因：仅已创建任务，尚未开始实现。

## 推荐执行顺序
1. 先清理 `target` 产物、确认 `.gitignore` 生效，避免测试输出混入提交。
2. 按批次补做最小必要验证。
3. 每个批次单独 `git add`、`git commit`。
4. 提交后同步创建或更新 PR。
5. PR 合并后关闭对应 issue。

## 下一步建议
- 先从 `批次 G` 和 `批次 A` 开始收口。
- 原因：
  - 改动范围相对集中。
  - 对后续服务批次是公共基础。
