# 一期自动化测试与验收基线说明

## 1. 目标

- 收口一期核心链路的自动化测试矩阵、执行命令与验收口径。
- 复用已有验证脚本与服务测试结果，统一输出可审计的测试报告。
- 作为 issue #27 的交付基线，支撑本地执行与 CI 对齐。

说明：

- 本文中的“自动化测试”默认指单元测试、集成测试、启动验证、验收脚本中的适用组合，不等于只跑单元测试。

## 2. 前置条件

- JDK：`21`（强制）
- Maven：`3.6.3+`
- 仓库根目录：`/Volumes/workspace/ChengDD`
- 可选环境变量：
  - `CDD_MAVEN_SETTINGS`
  - `CDD_MAVEN_REPO`
  - `CDD_PHASE1_TEST_MODULES`
  - `CDD_PHASE1_REPORT_FILE`

## 3. 一期核心链路测试矩阵

| 层级 | 核心链路 | 默认执行方式 | 验收要点 |
| --- | --- | --- | --- |
| 结构与边界 | 模块聚合、依赖方向、profile、迁移文件连续性、配置文件完整性 | `python3 scripts/validation/check_module_boundaries.py` | 模块边界无违规，配置资产齐全 |
| 构建与启动冒烟 | `validate/compile/install` + `auth/gateway/merchant` 启动 | `bash scripts/validation/validate_backend_skeleton.sh` | 构建通过，关键服务可启动 |
| 核心业务测试 | `common-security/auth/gateway/merchant/product/order/release/config` | `bash scripts/testing/run_phase1_acceptance.sh` | 模块测试报告全量生成且无失败 |
| 报告与验收 | 统一测试报告、达标结论、未执行项风险 | 自动输出到 `docs/05-delivery/reports/` | 报告字段完整、结论可追溯 |
| Nacos 契约 | 官方 Config/Discovery、加载优先级、失败策略、注销 | `bash scripts/testing/run_nacos_integration.sh` | 真实 Nacos 3 下契约测试与注销通过 |

## 4. 本地执行命令

执行一期验收（默认会先跑骨架校验，再跑模块测试，并输出报告）：

```bash
bash scripts/testing/run_phase1_acceptance.sh
```

仅复用已有测试结果并生成报告（不重跑 `mvn test`）：

```bash
bash scripts/testing/run_phase1_acceptance.sh --reuse-existing-results --skip-skeleton
```

仅执行骨架校验：

```bash
bash scripts/validation/validate_backend_skeleton.sh
```

## 5. CI 执行命令

当前 CI 工作流 `.github/workflows/backend-skeleton-validation.yml` 使用命令：

```bash
bash scripts/validation/validate_backend_skeleton.sh
```

当 CI 切换到一期验收全量口径时，命令应调整为：

```bash
bash scripts/testing/run_phase1_acceptance.sh
```

## 6. 测试报告模板与输出

- 模板文件：`docs/05-delivery/测试报告模板.md`
- 默认输出：`docs/05-delivery/reports/phase1-acceptance-latest.md`
- 报告必须包含：
  - 测试范围
  - 执行时间
  - 执行命令
  - 通过项
  - 失败项与失败原因
  - 关键日志或错误摘要
  - 是否达到当前任务验收标准
  - 未执行项与风险

## 7. 已复用的现有测试结果（2026-03-21）

已检测到 `surefire-reports` 共 `9` 份，统计 `tests=27, failures=0, errors=0, skipped=0`，覆盖：

- `cdd-common-security`
- `cdd-auth-service`
- `cdd-gateway`
- `cdd-merchant-service`
- `cdd-product-service`
- `cdd-order-service`
- `cdd-release-service`
- `cdd-config-service`

## 8. 当前验收通过口径

同时满足以下条件判定为通过：

- 模块边界校验通过
- 骨架校验脚本通过
- 一期模块测试无失败与错误
- 必选模块均存在 `surefire` 测试报告
- 测试报告完整输出并给出“是否达标”结论

## 9. 2026-08-09 Spring Cloud Alibaba 验收

版本基线为 Spring Boot `3.5.14`、Spring Cloud `2025.0.3`、Spring Cloud Alibaba `2025.0.0.0`、Nacos Client `3.0.3`。

最终复验命令：

```bash
git diff --check
python3 scripts/validation/check_module_boundaries.py
CDD_CONFIG_MODE=file mvn -f cdd-parent/pom.xml test
bash scripts/testing/run_nacos_integration.sh
bash -n scripts/local/*.sh scripts/nacos/*.sh scripts/testing/*.sh scripts/db/*.sh
```

Task 8 验收与本次最终门禁已取得以下功能证据：

- file 模式 30/30 reactor 成功，24 个套件共 102 项测试，failures=0、errors=0、skipped=0。
- Nacos 模式发布 11 个 DataId，Gateway 与九个服务 HTTP 健康 10/10，`CHENGDD` 组内 10 个应用均 hosts=1。
- Gateway 登录、auth/me、商品、订单、商家报表、功能开关均返回 HTTP 200、业务 code=0；令牌没有写入报告或仓库。
- file 模式静态路由回退已实测，未发生 Nacos 配置、注册或发现访问。
- 停止后 8080–8089 无监听，10 个应用均完成注销。
- Liquibase `4.31.1` classpath 迁移兼容已验证；已有数据库识别 30 个已执行 changeSet。

已知缺陷：10 个 Nacos 模式服务均在成功注销后记录 `NacosGracefulShutdownDelegate` ERROR/NPE。它未阻止本轮进程退出、端口清理或注册清空，但仍需跟进兼容性修复，不能表述为无错误停机。

生产 Nacos HA、鉴权、TLS、Nginx/HTTPS 和 CI/CD 部署尚未完成，本地 standalone 验收不能替代生产验收。
